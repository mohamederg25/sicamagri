# ── Server ──
PORT=3000
NODE_ENV=production

# ── Database ──
# Change this to your MongoDB URI (local or Atlas)
MONGO_URI=mongodb://localhost:27017/pepiniere

# ── Authentication ──
JWT_SECRET=sicamagri_prod_secret_key_2026
JWT_EXPIRE=7d

# ── CORS ──
CORS_ORIGIN=http://winicari.tn:3000
