# ── API Configuration ──
# En production, le frontend utilise /api (relatif, même serveur)
# Pour le développement local, décommentez la ligne ci-dessous
# VITE_API_URL=http://localhost:5000/api

# WebSocket URL (laisser vide = même serveur en production)
# Pour le développement local : VITE_SOCKET_URL=http://localhost:5000
